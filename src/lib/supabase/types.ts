// Placeholder until the Supabase project is linked. Regenerate the real types with:
//   npx supabase gen types typescript --project-id <project-ref> > src/lib/supabase/types.ts
// Using `any` here keeps the client generics happy in the meantime without faking a schema
// that could silently drift from supabase/migrations/*.sql.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any
